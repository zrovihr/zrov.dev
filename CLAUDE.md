# Project Rules

## !Excluded directory
The `!Excluded/` directory is **reference-only**. Files inside it are old Neocities archives excluded from the live website. Do **not** edit anything under `!Excluded/`.
